# Data Directory

Place local velocity models here. The default SEG/Salt path expected by the RTM runners is:

```text
data/seg676x230.bin
```

Large binary model files and generated wavefields are intentionally ignored by Git.
